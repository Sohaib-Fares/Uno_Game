import java.awt.Color;
import java.io.IOException;

import javax.swing.JFrame;

// import UI.Components.Buttons.MuButton;
// import UI.Components.Buttons.MuOutlinedButton;
import UI.Components.Frames.MuMainFrame;
import UI.Components.Textfields.MuTextField;

public class MainApp {
    public static void main(String[] args) throws IOException {
        // GameService service = new GameService();
        // service.showGameMenu();
        // ...
        new MuMainFrame();


        // JFrame frame = new JFrame("Test Frame");
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        // frame.setSize(400, 400);
        // frame.setLayout(null); // Use null layout for absolute positioning
        // frame.setVisible(true);
        // frame.setResizable(true);
        // frame.getContentPane().setBackground(Color.WHITE);
        // frame.setLocationRelativeTo(null); // Center the frame on the screen
        // frame.setTitle("JUNO");
        // frame.setIconImage(new javax.swing.ImageIcon("src/main/java/UI/Assets/JUNO.png").getImage());


        // for (int i = 0; i < 5; i++) {
        //     MuTextField textField = new MuTextField("Player " + (i + 1) + " name", 10);
        //     textField.setBounds(50, 50 + (i * 40), 300, 30); // Set position and size
        //     frame.add(textField);
        // }
        


    }
}
