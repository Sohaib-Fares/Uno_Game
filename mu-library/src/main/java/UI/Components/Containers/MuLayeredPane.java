package UI.Components.Containers;

import javax.swing.JLayeredPane;

/**
 * Wrapper for javax.swing.JLayeredPane.
 * A container that adds depth to a JFC/Swing container, allowing components
 * to overlap each other when needed.
 */
public class MuLayeredPane extends JLayeredPane {

    /**
     * Creates a new JLayeredPane.
     */
    public MuLayeredPane() {
        super();
    }
}